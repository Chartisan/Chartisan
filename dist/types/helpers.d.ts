import merge from 'deepmerge';
/**
 * Stores the default color palette.
 *
 * @export
 * @type {string[]}
 */
export declare const colorPalette: string[];
/**
 * Used to merge different nested options.
 *
 * @export
 * @type {typeof merge}
 */
export declare const mergeOptions: typeof merge;
